name: test-action-1
on: push
jobs:
  build:
  
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: Print Env Vars
      run: |
        set
        
    - name: second task
      run: | 
        pwd
        ls -l
